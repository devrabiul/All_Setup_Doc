# 🐳 Docker Architecture: কিভাবে কাজ করে Docker?

Docker কোনো একক সফটওয়্যার নয় — এটি একটি **client-server architecture**-এর উপর ভিত্তি করে কাজ করে।  
অর্থাৎ Docker তিনটি প্রধান অংশে ভাগ করা যায় 👇

---

## 🧩 ১. Docker Components Overview

| অংশ | কাজ |
|------|-----|
| **Docker Client** | ব্যবহারকারী যেটির মাধ্যমে Docker-এর সাথে যোগাযোগ করে |
| **Docker Daemon (Server)** | Container গুলো তৈরি, চালানো ও ম্যানেজ করে |
| **Docker Objects** | ইমেজ, কনটেইনার, ভলিউম, নেটওয়ার্ক ইত্যাদি |
| **Docker Registry** | যেখানে Docker image সংরক্ষণ ও শেয়ার করা হয় (যেমন: Docker Hub) |

---

## 🧠 Docker Architecture Diagram

```

┌─────────────────────────────────────────────────────┐
│                     User / CLI                     │
│                 (Docker Client)                    │
└─────────────────────────────────────────────────────┘
│   ↑
│   │
REST API    │   │   Commands
↓   │
┌─────────────────────────────────────────────────────┐
│               Docker Daemon (dockerd)              │
│ ┌────────────────────────────────────────────────┐ │
│ │  Container Runtime                             │ │
│ │  Image Builder                                 │ │
│ │  Network & Storage Manager                     │ │
│ └────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
│
│
↓
┌─────────────────────────────────────────────────────┐
│                 Docker Registry                     │
│        (Docker Hub / Private Registry)              │
└─────────────────────────────────────────────────────┘

````

---

## 🔹 ২. Docker Client

**Docker Client** হলো সেই ইন্টারফেস যার মাধ্যমে তুমি Docker-এর সাথে যোগাযোগ করো।

তুমি যখন কোনো কমান্ড দাও:
```bash
docker run nginx
````

এই কমান্ডটি Docker Client → Docker Daemon-এর কাছে পাঠায়,
Daemon সেটি এক্সিকিউট করে।

> 🧠 Client হতে পারে:
>
> * `docker CLI` (টার্মিনাল কমান্ড)
> * `Docker Desktop`
> * বা অন্য কোনো API Client

---

## 🔹 ৩. Docker Daemon (dockerd)

**Docker Daemon** হলো Docker-এর “engine” —
এটি মূল কাজগুলো করে যেমন:

* Container তৈরি / মুছা
* Image বিল্ড করা
* Network ও Volume ম্যানেজ করা
* Registry থেকে Image ডাউনলোড বা push করা

Daemon সবসময় ব্যাকগ্রাউন্ডে রান করে থাকে এবং Client-এর অনুরোধ (API call) গ্রহণ করে।

> 📍 Path: `/usr/bin/dockerd`

---

## 🔹 ৪. Docker Objects

Docker-এর মধ্যে কিছু মূল "Object" থাকে:

### 🧱 (a) Image

একটি **read-only template**, যা থেকে Container তৈরি হয়।
যেমন: `nginx:latest`, `mysql:8`, `php:8.2-fpm` ইত্যাদি।

### 📦 (b) Container

একটি **running instance** of an image।
প্রতিটি Container আলাদা environment-এ চলে।

```bash
docker run -d -p 80:80 nginx
```

👉 এখানে `nginx` image থেকে একটি Container তৈরি হলো।

### 💾 (c) Volume

Persistent data সংরক্ষণের জন্য ব্যবহার হয়।
Container বন্ধ হলেও Volume-এ ডেটা থেকে যায়।

```bash
docker volume create app_data
```

### 🌐 (d) Network

Container গুলোর মধ্যে যোগাযোগ (communication) নিশ্চিত করে।
Docker নিজেই নেটওয়ার্ক তৈরি ও ম্যানেজ করে।

---

## 🔹 ৫. Docker Registry

**Registry** হলো Docker image-এর “storage center”।

সবচেয়ে জনপ্রিয় Registry হলো 👉 [Docker Hub](https://hub.docker.com/)

তুমি চাইলেই নিজস্ব Private Registry ব্যবহার করতে পারো, যেমন:

* AWS ECR
* GitHub Container Registry
* GitLab Registry

### 📤 Push Example:

```bash
docker push username/myapp:latest
```

### 📥 Pull Example:

```bash
docker pull nginx:latest
```

---

## 🔹 ৬. কিভাবে Docker কাজ করে (Step-by-Step Flow)

1. 🧑‍💻 **তুমি কমান্ড দাও**

   ```bash
   docker run nginx
   ```

2. 💬 **Docker Client** সেই কমান্ড **Docker Daemon**-এ পাঠায়।

3. 🔍 **Daemon চেক করে** local image আছে কিনা। না থাকলে → Registry থেকে ডাউনলোড করে।

4. 📦 **Daemon সেই image থেকে container তৈরি করে।**

5. ⚙️ **Container রান হয়** আলাদা, isolated environment-এ।

6. 🌐 প্রয়োজনে Network/Volume attach হয়।

7. 🧭 তুমি এখন ব্রাউজারে গিয়ে দেখবে —
   `http://localhost` → Nginx চলছে 🎉

---

## 🔹 ৭. Docker Architecture-এর মূল ধারণা

| স্তর     | কাজ                                       |
| -------- | ----------------------------------------- |
| Client   | কমান্ড পাঠায়                              |
| Daemon   | Container manage করে                      |
| Registry | Image সংরক্ষণ করে                         |
| Objects  | Container, Image, Volume, Network গঠন করে |

---

## 🧠 সংক্ষেপে বললে:

> Docker = **Client + Daemon + Registry + Objects**
>
> Client কমান্ড দেয় → Daemon কাজ করে → Registry থেকে image নেয় → Container চালায়।

---

## 💡 উদাহরণ: Laravel App Flow

1. `docker build -t laravel-app .` → Image তৈরি
2. `docker run -d -p 8000:8000 laravel-app` → Container চালু
3. `http://localhost:8000` → Laravel অ্যাপ চলছে 🎉

---

## 🔚 উপসংহার

Docker একটি Client-Server মডেলে কাজ করে যেখানে:

* **Client** কমান্ড দেয়
* **Daemon** container চালায়
* **Registry** image সরবরাহ করে
* **Objects** মিলে তৈরি হয় containerized ecosystem

> 🧩 সহজভাবে বললে:
> **Docker হলো এমন একটি Engine, যা তোমার অ্যাপকে একদম আলাদা “বক্স”-এ চালায়, কিন্তু একদম একইভাবে যেকোনো সিস্টেমে।**