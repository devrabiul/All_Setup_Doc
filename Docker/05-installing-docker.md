# ⚙️ Docker ইনস্টলেশন ও প্রাথমিক কনফিগারেশন

Docker শেখার পরের ধাপ হলো —  
👉 তোমার কম্পিউটারে Docker ইনস্টল করা এবং সঠিকভাবে সেটআপ করা।

এই গাইডে তুমি জানতে পারবে:
- কীভাবে Windows / Linux / macOS-এ Docker ইনস্টল করতে হয়  
- ইনস্টলেশন যাচাই  
- প্রথম container চালানো

---

## 🧩 ১. ইনস্টলেশনের আগে যা জানা দরকার

| প্রয়োজনীয়তা | বিবরণ |
|--------------|--------|
| 💻 সিস্টেম | Windows 10/11, macOS, বা Linux |
| 🧠 জ্ঞান | Command line (টার্মিনাল/পাওয়ারশেল) ব্যবহার |
| 🔧 Software | Docker Desktop বা Docker Engine |
| ⚙️ Hardware | কমপক্ষে 4GB RAM ও 64-bit OS |

---

## 🪟 ২. Windows-এ Docker ইনস্টলেশন

### 🔹 ধাপ ১: Docker Desktop ডাউনলোড
👉 [https://www.docker.com/products/docker-desktop](https://www.docker.com/products/docker-desktop)

Windows-এর জন্য `.exe` ফাইলটি ডাউনলোড করো।

### 🔹 ধাপ ২: ইনস্টল ও চালু করা
ইনস্টলেশনের সময়:
- **WSL 2 Backend** সক্ষম রাখো (Enable)  
- তারপর Finish দিয়ে Docker Desktop চালু করো।

### 🔹 ধাপ ৩: যাচাই করা
PowerShell বা CMD খুলে রান করো:
```bash
docker --version
````

✅ যদি ভার্সন দেখায়, তবে ইনস্টল সফল।

### 🔹 ধাপ ৪: টেস্ট Container চালানো

```bash
docker run hello-world
```

> 🧠 এই কমান্ডটি একটি ছোট টেস্ট container চালায়
> যা যাচাই করে Docker সঠিকভাবে কাজ করছে কিনা।

---

## 🐧 ৩. Linux-এ Docker ইনস্টলেশন

Docker Linux-এর জন্য **Docker Engine** হিসেবে ইনস্টল হয়।

### 🔹 ধাপ ১: পুরনো ভার্সন মুছে ফেলা

```bash
sudo apt remove docker docker-engine docker.io containerd runc
```

### 🔹 ধাপ ২: প্রয়োজনীয় প্যাকেজ ইনস্টল

```bash
sudo apt update
sudo apt install ca-certificates curl gnupg lsb-release
```

### 🔹 ধাপ ৩: Docker-এর অফিসিয়াল GPG key যোগ করা

```bash
sudo mkdir -p /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
```

### 🔹 ধাপ ৪: Repository যোগ করা

```bash
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] \
  https://download.docker.com/linux/ubuntu \
  $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
```

### 🔹 ধাপ ৫: Docker Engine ইনস্টল

```bash
sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
```

### 🔹 ধাপ ৬: Docker চালানো

```bash
sudo systemctl start docker
sudo systemctl enable docker
```

### 🔹 ধাপ ৭: যাচাই করা

```bash
docker --version
docker run hello-world
```

✅ সবকিছু ঠিক থাকলে “Hello from Docker!” বার্তা দেখাবে।

---

## 🍎 ৪. macOS-এ Docker ইনস্টলেশন

### 🔹 ধাপ ১: Docker Desktop ডাউনলোড

👉 [https://www.docker.com/products/docker-desktop](https://www.docker.com/products/docker-desktop)

macOS (Intel / Apple Silicon) অনুযায়ী `.dmg` ফাইলটি বেছে নাও।

### 🔹 ধাপ ২: ইনস্টল ও চালানো

`.dmg` ফাইল খুলে “Applications” ফোল্ডারে টেনে দাও।
তারপর **Docker Desktop** চালাও।

### 🔹 ধাপ ৩: যাচাই করা

Terminal-এ রান করো:

```bash
docker --version
docker run hello-world
```

✅ সফল ইনস্টলেশন হলে Docker একটি টেস্ট container চালাবে।

---

## 🧠 ৫. ইনস্টলেশন যাচাই

সব সিস্টেমেই নিচের কমান্ড দিয়ে পরীক্ষা করা যায়:

```bash
docker info
```

এটি Docker-এর সম্পূর্ণ তথ্য দেখাবে —
যেমন containers, images, storage driver, version ইত্যাদি।

---

## 🐳 ৬. প্রথম বাস্তব কনটেইনার চালানো

চলো এখন একটি nginx web server container চালাই 👇

```bash
docker run -d -p 8080:80 nginx
```

> * `-d` → detached mode (background-এ চলে)
> * `-p 8080:80` → Host-এর 8080 port map করা container-এর 80 port-এ
> * `nginx` → official nginx image

এখন ব্রাউজারে গিয়ে খোলো:

```
http://localhost:8080
```

🎉 তুমি দেখবে “Welcome to nginx!” পেজ।

---

## 🧹 ৭. Docker বন্ধ করা ও রিমুভ করা

### Container বন্ধ করা

```bash
docker stop container_id
```

### Container মুছে ফেলা

```bash
docker rm container_id
```

### সব বন্ধ/রিমুভ করা (optional)

```bash
docker stop $(docker ps -aq)
docker rm $(docker ps -aq)
```

---

## ⚡ ৮. ইনস্টলেশনের পর যা করো

1. Docker Desktop চালু রাখো
2. `docker compose` কমান্ড চর্চা করো
3. কিছু প্র্যাকটিস প্রজেক্ট রান করো

   ```bash
   docker run redis
   docker run mysql
   docker run phpmyadmin
   ```

---

## 🧭 ৯. Troubleshooting টিপস

| সমস্যা                    | সমাধান                                                             |
| ------------------------- | ------------------------------------------------------------------ |
| Docker command চলে না     | নিশ্চিত হও Docker Desktop চালু আছে                                 |
| Permission denied (Linux) | ইউজারকে docker group-এ যোগ করো:<br>`sudo usermod -aG docker $USER` |
| Port conflict             | অন্য অ্যাপ ওই পোর্ট ব্যবহার করছে কিনা দেখো                         |
| Slow startup              | BIOS-এ virtualization সক্রিয় আছে কিনা চেক করো                      |

---

## ✅ সংক্ষেপে

| ধাপ | কাজ                            |
| --- | ------------------------------ |
| 1️⃣ | Docker Desktop / Engine ইনস্টল |
| 2️⃣ | `docker --version` দিয়ে যাচাই  |
| 3️⃣ | `docker run hello-world` চালাও |
| 4️⃣ | প্রথম nginx container রান করো  |
| 5️⃣ | Docker Compose শেখা শুরু করো   |

---

> 💡 **উপসংহার:**
> Docker ইনস্টল করা একবারের কাজ —
> তারপর তুমি এক কমান্ডেই সার্ভার, ডাটাবেস, বা পুরো অ্যাপ চালাতে পারবে।
> *এটাই Docker-এর আসল শক্তি।* 🐳