# 🔐 Ubuntu সার্ভারের জন্য SSH ও রিমোট অ্যাক্সেস (সহজ বাংলায়)

**SSH (Secure Shell)** হলো একটি নিরাপদ প্রোটোকল যা ব্যবহার করে আপনি আপনার কম্পিউটার থেকে দূরের Linux সার্ভারে সংযোগ করতে পারেন।  
এই গাইডে আপনি শিখবেন কিভাবে SSH দিয়ে সার্ভারে লগইন করবেন, ফাইল ট্রান্সফার করবেন, ও সার্ভার ম্যানেজ করবেন — নিরাপদভাবে এবং সহজভাবে।

---

## 🧭 1. SSH কী?

SSH হলো একটি **নেটওয়ার্ক প্রোটোকল** যা আপনার লোকাল কম্পিউটার এবং রিমোট সার্ভারের মধ্যে এনক্রিপ্টেড (সুরক্ষিত) যোগাযোগ স্থাপন করে। অর্থাৎ, কেউ মাঝখানে থেকে আপনার তথ্য চুরি করতে পারবে না।

### SSH ব্যবহারের মূল উদ্দেশ্য:
- 🔹 দূর থেকে সার্ভারে লগইন করা  
- 🔹 সার্ভারে কমান্ড চালানো  
- 🔹 ফাইল আপলোড বা ডাউনলোড করা  
- 🔹 সার্ভারের কনফিগারেশন ম্যানেজ করা  

💡 সহজভাবে বলতে গেলে — SSH হলো আপনার সার্ভারের "রিমোট কন্ট্রোল"।

---

## 🌐 2. SSH দিয়ে সার্ভারে কানেক্ট করা

### 🔑 PEM Key ব্যবহার করে (AWS EC2 বা অন্যান্য ক্লাউড সার্ভার)

1. প্রথমে আপনার সার্ভার অ্যাডমিন আপনাকে একটি `.pem` ফাইল দেবেন — এটি একটি **Private Key**, যা আপনার সার্ভারে নিরাপদে প্রবেশের চাবি।
2. সেই কী ফাইলের পারমিশন ঠিক করতে হবে (অন্য কেউ যেন এটি পড়তে না পারে):

```bash
chmod 400 botkey.pem
```

3. এখন সার্ভারে কানেক্ট করুন:
```bash
ssh -i botkey.pem ubuntu@server_ip
```

- `ubuntu` → Ubuntu সার্ভারের ডিফল্ট ইউজারনেম  
- `server_ip` → সার্ভারের পাবলিক IP ঠিকানা  

### উদাহরণ:
```bash
ssh -i botkey.pem ubuntu@54.82.111.59
```

✅ সফল হলে আপনি সার্ভারের টার্মিনালে প্রবেশ করবেন, যেখানে সব কমান্ড Linux অনুযায়ী কাজ করবে।

---

### 🔒 পাসওয়ার্ড দিয়ে লগইন করা

যদি আপনার সার্ভার **Password Authentication** ব্যবহার করে, তাহলে:
```bash
ssh username@server_ip
```
তারপর পাসওয়ার্ড লিখে Enter চাপুন।

💡 কিন্তু মনে রাখবেন — পাসওয়ার্ড পদ্ধতি অপেক্ষাকৃত **কম নিরাপদ**। PEM বা SSH key ব্যবহার করাই ভালো।

---

## 💻 3. SSH এর ভিতরে কিছু দরকারি কমান্ড

সার্ভারে ঢোকার পর আপনি নিচের Linux কমান্ডগুলো ব্যবহার করতে পারবেন:

| কমান্ড | কাজ |
|--------|------|
| `pwd` | বর্তমান ডিরেক্টরি দেখায় |
| `ls` | ফাইল লিস্ট করে |
| `cd /path` | অন্য ডিরেক্টরিতে যান |
| `nano file.txt` | ফাইল তৈরি বা এডিট করুন |
| `sudo` | অ্যাডমিন হিসেবে কমান্ড চালান |

💡 **টিপস:** যদি আপনি ফাইল এডিট করতে চান, তাহলে `nano` বা `vim` ব্যবহার করতে পারেন।

---

## 🚪 4. SSH থেকে বের হওয়া

সার্ভার থেকে লগআউট করতে:
```bash
exit
```
অথবা কিবোর্ডে `Ctrl + D` চাপুন।

---

## 🗝️ 5. SSH কী ম্যানেজমেন্ট (Key Management)

SSH কী আপনাকে **পাসওয়ার্ড ছাড়াই নিরাপদ লগইন** করতে দেয়।

### 🔧 নিজের SSH কী তৈরি করা

```bash
ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
```

এটি দুটি ফাইল তৈরি করবে:
- প্রাইভেট কী: `~/.ssh/id_rsa`  
- পাবলিক কী: `~/.ssh/id_rsa.pub`

### সার্ভারে পাবলিক কী যোগ করা

```bash
ssh-copy-id -i ~/.ssh/id_rsa.pub ubuntu@server_ip
```

এবার থেকে আপনি `ssh ubuntu@server_ip` দিয়ে সরাসরি লগইন করতে পারবেন — পাসওয়ার্ড ছাড়াই ✅

💡 **Extra Tip:** আপনি `.ssh/config` ফাইল ব্যবহার করে একাধিক সার্ভারের সেটিংস এক জায়গায় রাখতে পারেন।

---

## 📤 6. ফাইল ট্রান্সফার (SCP ও SFTP ব্যবহার করে)

### 🔁 SCP (Secure Copy)

লোকাল কম্পিউটার থেকে সার্ভারে ফাইল পাঠাতে:
```bash
scp -i botkey.pem localfile.txt ubuntu@server_ip:/remote/path/
```

পুরো ফোল্ডার পাঠাতে:
```bash
scp -i botkey.pem -r /local/folder ubuntu@server_ip:/remote/path/
```

সার্ভার থেকে ফাইল ডাউনলোড করতে:
```bash
scp -i botkey.pem ubuntu@server_ip:/remote/path/file.txt /local/path/
```

### 🧩 SFTP (Secure File Transfer Protocol)

ইন্টারঅ্যাকটিভ মোডে ফাইল ট্রান্সফার করতে:
```bash
sftp -i botkey.pem ubuntu@server_ip
```

SFTP এর ভিতরে কিছু দরকারি কমান্ড:
| কমান্ড | কাজ |
|--------|------|
| `ls` | সার্ভারের ফাইল লিস্ট |
| `cd folder` | ফোল্ডার পরিবর্তন |
| `get file.txt` | সার্ভার থেকে ফাইল নামানো |
| `put file.txt` | সার্ভারে ফাইল পাঠানো |
| `exit` | বের হওয়া |

---

## 🧠 7. SSH কনফিগারেশন টিপস

- 🔐 শক্তিশালী SSH কী ব্যবহার করুন, শুধুমাত্র পাসওয়ার্ড নয়  
- 🚫 `root` ইউজার লগইন নিষ্ক্রিয় রাখুন (`/etc/ssh/sshd_config` এ সেট করা যায়)  
- ⚙️ ডিফল্ট SSH পোর্ট (২২) পরিবর্তন করে অন্য পোর্ট দিন — যেমন 2222  
- 🧩 সংযোগে সমস্যা হলে ব্যবহার করুন:
  ```bash
  ssh -v ubuntu@server_ip
  ```
  এটি ডিবাগ মোডে বিস্তারিত তথ্য দেখাবে।

---

## 🏁 8. সারসংক্ষেপ

SSH শেখা মানে সার্ভার ব্যবস্থাপনার প্রথম দরজা খোলা।  
এর মাধ্যমে আপনি পারবেন:

- 🔹 সার্ভারে নিরাপদে লগইন করতে  
- 🔹 দূর থেকে কমান্ড চালাতে  
- 🔹 ফাইল পাঠানো ও নামাতে  
- 🔹 অ্যাপ ডিপ্লয় করতে  

💡 **SSH** ছাড়া কোনো সার্ভার অ্যাডমিন বা ডেভেলপার কাজ এগিয়ে নিতে পারেন না।  
এটি আপনার **LAMP stack**, **PHP deployment**, বা **Laravel server setup** শেখার জন্য ভিত্তি তৈরি করে।