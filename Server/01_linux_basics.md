# 💻 Linux এর বেসিক গাইড (নবীনদের জন্য সহজ ভাষায়)

এই গাইডটি তৈরি করা হয়েছে **Linux** এর মূল ধারণা শেখানোর জন্য, বিশেষ করে **Ubuntu** ভিত্তিক সিস্টেমের উপর। অনেক সার্ভার — যেমন AWS, Google Cloud, DigitalOcean — সবই Linux ব্যবহার করে।  
যারা নতুন এবং কমান্ড লাইন (Command Line Interface - CLI) ব্যবহার শিখতে চান, এটি তাদের জন্য একদম উপযুক্ত শুরু।

---

## 🧭 1. Linux কী?

Linux হলো একটি **open-source** অপারেটিং সিস্টেম যা **Unix** এর উপর ভিত্তি করে তৈরি। অর্থাৎ, এর কোড সবার জন্য উন্মুক্ত, কেউ চাইলে এটি ব্যবহার, পরিবর্তন বা নিজের মতো করে বানাতে পারে।

### Linux-এর কিছু জনপ্রিয় বৈশিষ্ট্য:
- 🆓 **ফ্রি ও ওপেন-সোর্স** – কোনো লাইসেন্স ফি লাগে না।  
- 🔒 **নিরাপদ ও স্থিতিশীল** – ভাইরাস বা ক্র্যাশের ঝুঁকি কম।  
- 👥 **মাল্টি-ইউজার সাপোর্ট** – একাধিক ইউজার একসাথে কাজ করতে পারে।  
- ⚙️ **CLI ও GUI দুটোই আছে** – সার্ভারে সাধারণত CLI বেশি ব্যবহার হয় কারণ এটি দ্রুত ও কার্যকর।

💡 **মনে রাখুন:** সার্ভারে কাজের বেশিরভাগই কমান্ড লাইন দিয়ে করা হয় — যেমন ফাইল ম্যানেজমেন্ট, সার্ভিস রিস্টার্ট, পারমিশন সেট করা ইত্যাদি।

---

## 🔗 2. Linux সার্ভারে সংযোগ (SSH ব্যবহার করে)

Linux সার্ভারে দূর থেকে কানেক্ট করতে সাধারণত **SSH (Secure Shell)** ব্যবহার করা হয়।  
এটি একটি সিকিউর চ্যানেল যার মাধ্যমে আপনি আপনার কম্পিউটার থেকে সার্ভারে লগইন করতে পারেন।

### সংযোগের কমান্ড:
```bash
ssh -i your_key.pem username@server_ip
```

- `your_key.pem` → আপনার প্রাইভেট কী ফাইল  
- `username` → সাধারণত `ubuntu` বা `root`  
- `server_ip` → সার্ভারের পাবলিক IP ঠিকানা  

### উদাহরণ:
```bash
ssh -i botkey.pem ubuntu@54.82.111.59
```

💡 যদি "Permission denied" দেখায়, তাহলে প্রাইভেট কী ফাইলের পারমিশন ঠিক করতে হতে পারে:
```bash
chmod 400 your_key.pem
```

---

## 📂 3. ফাইল ও ডিরেক্টরি ম্যানেজমেন্ট

Linux এ সবকিছুই একটি ফাইল বা ডিরেক্টরি আকারে থাকে। তাই ফাইল ম্যানেজমেন্ট শেখা খুব গুরুত্বপূর্ণ।

### ফাইল দেখা (Listing files)
```bash
ls        # বর্তমান ডিরেক্টরির ফাইল দেখায়
ls -l     # বিস্তারিতভাবে দেখায় (পারমিশন, মালিক, সাইজ, তারিখ)
ls -a     # হিডেন ফাইলসহ সব ফাইল দেখায়
```

### ডিরেক্টরি পরিবর্তন (Changing directory)
```bash
cd /path/to/folder    # নির্দিষ্ট ফোল্ডারে যান
cd ..                 # এক ধাপ পিছনে যান
cd ~                  # হোম ডিরেক্টরিতে যান
```

### নতুন ফোল্ডার তৈরি
```bash
mkdir myfolder        # নতুন ফোল্ডার তৈরি
mkdir -p a/b/c        # একাধিক লেভেল একসাথে তৈরি
```

### নতুন ফাইল তৈরি ও সম্পাদনা
```bash
touch file.txt        # খালি ফাইল তৈরি
nano file.txt         # Nano এডিটরে ফাইল খুলুন/এডিট করুন
```

💡 Nano থেকে বের হতে `Ctrl + X`, তারপর `Y`, তারপর `Enter` চাপুন।

### ফাইল কপি ও মুভ
```bash
cp old.txt new.txt        # ফাইল কপি
cp -r folder1 folder2     # ফোল্ডারসহ কপি
mv oldname.txt newname.txt # ফাইলের নাম পরিবর্তন বা স্থানান্তর
```

### ফাইল মুছে ফেলা
```bash
rm file.txt         # ফাইল ডিলিট
rm -r folder        # ফোল্ডারসহ সব মুছে ফেলে
```

---

## 🔐 4. ফাইল পারমিশন বোঝা

Linux এ প্রতিটি ফাইল বা ফোল্ডারের জন্য তিন ধরনের পারমিশন থাকে:

- **r (read)** → পড়ার অনুমতি  
- **w (write)** → পরিবর্তন করার অনুমতি  
- **x (execute)** → চালানোর অনুমতি  

### পারমিশন দেখা
```bash
ls -l
```
এখানে আপনি এমন কিছু দেখতে পাবেন:
```
-rwxr-xr--  1 user user  1234 Nov 1  file.sh
```

### পারমিশন পরিবর্তন
```bash
chmod 755 file.sh     # মালিক: পড়া, লেখা, চালানো | অন্যরা: পড়া ও চালানো
chmod 644 file.txt    # মালিক: পড়া, লেখা | অন্যরা: শুধু পড়া
```

### মালিক পরিবর্তন
```bash
sudo chown user:group file.txt
```

💡 উদাহরণ: `sudo chown ubuntu:www-data file.txt`

---

## ⚙️ 5. প্রসেস ও সার্ভিস ম্যানেজমেন্ট

Linux সার্ভারে অনেক প্রোগ্রাম একসাথে চলে — একে বলে **process**।

### চলমান প্রসেস দেখা
```bash
ps aux        # সব প্রসেসের লিস্ট
top           # লাইভ মনিটরিং
htop          # আরও সুন্দর ইন্টারফেস (ইনস্টল করতে হবে)
```

### সার্ভিস ম্যানেজমেন্ট (যেমন Apache, Nginx)
```bash
sudo systemctl start apache2      # সার্ভিস চালু
sudo systemctl stop apache2       # বন্ধ
sudo systemctl restart apache2    # রিস্টার্ট
sudo systemctl status apache2     # স্ট্যাটাস চেক
```

💡 অনেক সার্ভিস `systemctl` কমান্ডের মাধ্যমে নিয়ন্ত্রণ করা যায়।

---

## 📦 6. প্যাকেজ ম্যানেজমেন্ট (APT)

Ubuntu তে সফটওয়্যার ইনস্টল বা আপডেট করতে ব্যবহৃত হয় **APT (Advanced Package Tool)**।

### সিস্টেম আপডেট
```bash
sudo apt update
sudo apt upgrade -y
```

### সফটওয়্যার ইনস্টল
```bash
sudo apt install nginx
```

### সফটওয়্যার রিমুভ
```bash
sudo apt remove nginx
```

💡 `apt list --installed` দিয়ে ইনস্টল করা সব প্যাকেজ দেখতে পারেন।

---

## 🧰 7. দরকারি কমান্ড লিস্ট

| কমান্ড | কাজ |
|--------|------|
| pwd | আপনি বর্তমানে কোন ডিরেক্টরিতে আছেন তা দেখায় |
| whoami | লগইন করা ইউজারের নাম |
| hostname | সার্ভারের নাম |
| df -h | ডিস্ক ব্যবহারের অবস্থা |
| free -h | RAM ব্যবহারের অবস্থা |
| uname -a | অপারেটিং সিস্টেমের তথ্য |
| history | আগের কমান্ডগুলোর তালিকা |
| clear | টার্মিনাল পরিষ্কার করা |

---

## 🌱 8. নতুনদের জন্য টিপস

- 🔤 কমান্ড টাইপ করার সময় **Tab** চাপলে অটো-কমপ্লিট হবে।  
- 📘 কোনো কমান্ড বুঝতে না পারলে `man command` লিখে ম্যানুয়াল দেখুন।  
  উদাহরণ: `man ls`  
- ⚡ প্রশাসনিক (admin/root) কাজের জন্য সবসময় `sudo` ব্যবহার করুন।  
- 🧠 চেষ্টা করুন কমান্ডের মানে মনে রাখতে — যেমন `cd` মানে *change directory*।

---

## 🏁 সারসংক্ষেপ

Linux হলো সার্ভার জগতের মেরুদণ্ড। এর বেসিক কমান্ড, ফাইল ম্যানেজমেন্ট, পারমিশন, ও প্যাকেজ ম্যানেজমেন্ট জানলে আপনি সহজেই সার্ভারে কাজ করতে পারবেন।  
ধীরে ধীরে অভ্যেস করুন CLI ব্যবহার করার — এটি একবার আত্মবিশ্বাসের সাথে শিখে ফেললে, আপনি আপনার সার্ভার নিজের মতো নিয়ন্ত্রণ করতে পারবেন।

🚀 পরবর্তী ধাপে আপনি শিখতে পারেন:
- SSH কী ম্যানেজমেন্ট  
- ইউজার ও গ্রুপ ম্যানেজমেন্ট  
- ওয়েব সার্ভার সেটআপ (Apache, Nginx)